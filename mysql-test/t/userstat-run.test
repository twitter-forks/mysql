SET GLOBAL userstat=ON;
SET GLOBAL thread_statistics=ON;

# connect (other,localhost,root,,test);
# connection other;

SELECT 1;

CREATE TABLE tttess (a INT PRIMARY Key,
b INT, key (b)) engine=InnoDB;

insert into tttess (a, b) values (1,1), (2,1), (3,2), (4,2);

select * from tttess;

# connection default;

# disconnect other;

vertical_results;
SELECT * FROM INFORMATION_SCHEMA.TABLE_STATISTICS;
SELECT * FROM INFORMATION_SCHEMA.CLIENT_STATISTICS;
SELECT * FROM INFORMATION_SCHEMA.INDEX_STATISTICS;
SELECT * FROM INFORMATION_SCHEMA.TABLE_STATISTICS WHERE TABLE_NAME = 'tttess';
SELECT * FROM INFORMATION_SCHEMA.THREAD_STATISTICS;
SELECT * FROM INFORMATION_SCHEMA.USER_STATISTICS;

SHOW CLIENT_STATISTICS;
SHOW INDEX_STATISTICS;
SHOW TABLE_STATISTICS;
SHOW THREAD_STATISTICS;
SHOW USER_STATISTICS;
horizontal_results;

FLUSH CLIENT_STATISTICS;
FLUSH INDEX_STATISTICS;
FLUSH TABLE_STATISTICS;
FLUSH THREAD_STATISTICS;
FLUSH USER_STATISTICS;

DROP TABLE tttess;

SET GLOBAL userstat=OFF;
SET GLOBAL thread_statistics=OFF;

